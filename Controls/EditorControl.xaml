<UserControl x:Class="OctoCapture.Controls.EditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="ToolBtn" TargetType="RadioButton">
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="Background" Value="#3A3A40"/>
            <Setter Property="BorderBrush" Value="#55555C"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2F6BAF"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#2F9BFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#2F9BFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActBtn" TargetType="Button">
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="Background" Value="#3A3A40"/>
            <Setter Property="BorderBrush" Value="#55555C"/>
        </Style>
        <Style x:Key="Swatch" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="1,0"/>
            <Setter Property="BorderBrush" Value="#77777C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#2A2A2E" Padding="6,4" CornerRadius="4,4,0,0">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,2,10,2">
                    <RadioButton x:Name="BtnPen" Style="{StaticResource ToolBtn}" GroupName="edittools" Content="✏ 펜" IsChecked="True" Checked="Tool_Checked" Tag="Pen"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="─ 직선" Checked="Tool_Checked" Tag="Line"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="➤ 화살표" Checked="Tool_Checked" Tag="Arrow"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="▭ 사각형" Checked="Tool_Checked" Tag="Rect"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="◯ 원" Checked="Tool_Checked" Tag="Ellipse"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="T 텍스트" Checked="Tool_Checked" Tag="Text"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="▩ 모자이크" Checked="Tool_Checked" Tag="Mosaic"/>
                    <RadioButton Style="{StaticResource ToolBtn}" GroupName="edittools" Content="✂ 자르기" Checked="Tool_Checked" Tag="Crop"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,2,10,2">
                    <Border x:Name="CurrentColorBox" Width="22" Height="22" CornerRadius="4"
                            BorderBrush="White" BorderThickness="2" Background="Red" Margin="0,0,5,0"/>
                    <Button Style="{StaticResource Swatch}" Background="#FF3B30" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#FF9500" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#FFCC00" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#34C759" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#2F9BFF" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#AF52DE" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#FF2D92" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#000000" Click="Swatch_Click"/>
                    <Button Style="{StaticResource Swatch}" Background="#FFFFFF" Click="Swatch_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,2,10,2">
                    <TextBlock Text="굵기" Foreground="#BBBBBB" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <Slider x:Name="ThicknessSlider" Width="80" Minimum="1" Maximum="16" Value="3"
                            VerticalAlignment="Center" IsSnapToTickEnabled="True" TickFrequency="1"/>
                    <TextBlock x:Name="ThicknessLabel" Text="3" Foreground="#BBBBBB" VerticalAlignment="Center" Width="18" Margin="4,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                    <Button Style="{StaticResource ActBtn}" Content="↩ 실행취소" Click="Undo_Click"/>
                    <Rectangle Width="1" Fill="#55555C" Margin="5,2"/>
                    <Button Style="{StaticResource ActBtn}" Content="💾 저장" ToolTip="현재 캡쳐에 덮어쓰기" Click="SaveOverwrite_Click"/>
                    <Button Style="{StaticResource ActBtn}" Content="＋ 새 캡쳐 저장" ToolTip="캡쳐 목록에 새 항목으로 추가" Click="SaveAsNew_Click"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#2A2A2E" Padding="8,3" CornerRadius="0,0,4,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusSize" Foreground="#88888E" FontSize="11"/>
                <TextBlock x:Name="StatusZoom" Foreground="#88888E" FontSize="11" Margin="12,0,0,0"/>
                <TextBlock Text="Ctrl+휠: 확대/축소  ·  Ctrl+Z: 실행취소" Foreground="#66666C" FontSize="11" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer x:Name="Scroller" Background="#151517"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                      PreviewMouseWheel="Scroller_PreviewMouseWheel">
            <Border x:Name="ZoomHost" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12">
                <Grid x:Name="EditRoot" Background="Transparent"
                      MouseLeftButtonDown="EditRoot_MouseDown"
                      MouseMove="EditRoot_MouseMove"
                      MouseLeftButtonUp="EditRoot_MouseUp">
                    <Image x:Name="BaseImage" Stretch="Fill"/>
                    <Canvas x:Name="ShapeCanvas" Background="Transparent"/>
                </Grid>
            </Border>
        </ScrollViewer>
    </DockPanel>
</UserControl>
